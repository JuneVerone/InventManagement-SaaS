# client/.env
# Vite only exposes variables that start with VITE_ to your frontend code.
# Access them with: import.meta.env.VITE_API_URL
#
# NEVER put secrets here — this is bundled into the JS the browser downloads.
# It is visible to anyone who opens DevTools.

VITE_API_URL=http://localhost:3000/api